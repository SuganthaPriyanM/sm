CREATE OR REPLACE DATABASE mock 's';

ALTER DATABASE mock
  CHARSET DEFAULT;

ALTER DATABASE mock
  CACHE ON (
    MAINTAINERPERIOD 21600
    TIMETOLIVE 3600
    DATASOURCE ds_postgres DATABASE test
  );

